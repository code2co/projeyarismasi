<img src="<%= asset_url("ll.png") %>" alt="Logo Image">
<h2>Yeni Üye Kaydı</h2>
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
<%= f.error_notification %>
<% resource.errors.messages.each do |k,message| %>
  <%= "#{k} #{message[0]}" %><br>
<% end %>
<div class="form-inputs">
  <%= f.input :name, required: true, autofocus: true %>
  <%= f.input :email, required: true %>
  <%= f.input :password, required: true, hint: ("#{@minimum_password_length} karakter içermelidir" if @minimum_password_length) %>
  <%= f.input :university, collection: ['Balıkesir Üniversitesi', 'Bursa Teknik Üniversitesi', 'Uludağ Üniversitesi'], label: "Üniversite Seçimi", prompt: "Üniversitenizi Seçin" %>
  <%= f.input :department, label: "Bölüm Seçimi", placeholder: "Bölümünüzü girin" %>
  <%= f.input :mobile_phone, label: "Cep Telefonu", placeholder: "Cep Telefonunuzu Girin" %>
  <%= f.input :role, collection: {applicant: "Projeye Başvurmak İstiyorum", volunteer: "Gönüllü Çalışmak istiyorum"}.map { |k,v| [v,k] }, prompt: "Bu sitedeki rolünüz ne olacak?", label: "Rol Seçimi"%>
</div>
<div class="form-actions">
  <%= f.button :submit, I18n.t("devise.registrations.new") %>
</div>
<% end %>
  
<%= render "devise/shared/links" %>
