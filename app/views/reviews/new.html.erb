<%= render "application/breadcrumb" %>
<%= simple_form_for [@admission, @review] do |f| %>
<%= f.input :user_id, as: :select, collection: User.all, label_method: :email, value_method: :id %>
<%= f.button :submit, "Değerlendirme oluştur ve hakeme gönder" %>
<% end %>
